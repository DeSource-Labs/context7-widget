import { Directive } from '@angular/core';

/** Marks projected content used by the Angular-managed widget trigger. */
@Directive({
  selector: '[context7WidgetTrigger]',
  standalone: true
})
export class Context7WidgetTrigger {}
